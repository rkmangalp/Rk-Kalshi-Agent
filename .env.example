# Copy to .env in the repo root (gitignored). Never commit this file.
# Dashboard Connect reads these two variables — keys are not typed in the UI.
# Connecting is read-only. It does NOT enable live order placement.

KALSHI_API_KEY_ID=
KALSHI_PRIVATE_KEY_PATH=C:\Users\Rk\.kalshi\kalshi.key
KALSHI_ENVIRONMENT=prod
# prod  → https://external-api.kalshi.com/trade-api/v2
# demo  → https://external-api.demo.kalshi.co/trade-api/v2
